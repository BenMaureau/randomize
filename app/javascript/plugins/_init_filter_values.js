const filterValues = () => {
  const budgetField = document.getElementById('max-price');
  const budget = budgetField.value;
  const btnRandom = document.getElementById('rdm-btn');
  // const indoorOutdoorSelectors = document.querySelectorAll('.select-indoor-outdoor');
  // console.log(indoorOutdoorSelectors)

  // récup tous les prix toutes les activités (@activities)
  // récup la valeur du budget max (input)
  // filtrer en fonction de la valeur récupérée

  // axios.post('/custom_activity', {budget: })

  // if (indoorOutdoorSelectors.classList.contains('active-indoor-outdoor-card')) {
  //   indoorOutdoorSelectors.classList.remove('display-none');
   }

export { filterValues }
